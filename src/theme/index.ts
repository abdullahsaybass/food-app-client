export { Colors } from './color';
export { Typography, FontFamily, FontSize } from './typography';
export { Spacing, Radius } from './spacing';

// Convenience re-export so imports are always from '@theme'
// Set up module alias: '@theme' → 'src/theme' in tsconfig + babel.config